{% extends "base.html.twig" %}


{% block title %}
	Suppression de la fiche de
	{{owner.firstName}}
	{{owner.lastName}}
{% endblock %}

{% block body %}
	<ol class="breadcrumb">
		<li class="breadcrumb-item">
			<a href="{{path('homepage')}}">Accueil</a>
		</li>
		<li class="breadcrumb-item">
			<a href="{{path('owner_list')}}">Propriétaires</a>
		</li>
		<li class="breadcrumb-item active">Suppression</li>
	</ol>

	<h1 class="mb-4">Suppression de la fiche de
		{{owner.firstName}}
		{{owner.lastName}}</h1>

	{{ form_start(formView)}}

	{{ form_errors(formView)}}

	{{ form_widget(formView)}}
	<a class="btn btn-primary" href="{{urlGenerator.generate('owner_show', {'id': owner.id }) }}">Retour</a>
	<button type="submit" class="btn btn-danger">Je confirme</button>
	{{ form_end(formView)}}
{% endblock body %}
